{
  "mcpServers": {
    "sleeper-fantasy": {
      "command": "docker",
      "args": [
        "compose",
        "-f",
        "/absolute/path/to/sleeper-mcp/docker-compose.yml",
        "run",
        "--rm",
        "-i",
        "sleeper-mcp"
      ]
    }
  }
}
