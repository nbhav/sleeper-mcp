interface:
  display_name: "Sleeper Weekly Scout"
  short_description: "Back-test weeks and actionable waivers"
  default_prompt: "Use $sleeper-weekly-scout to call deterministic MCP tools for Sleeper leaders, lineup decisions, and actionable waiver targets. Fall back only to Dockerized repo commands, never host installs, use make local-up when setup is needed, and run make teardown after workflow checks."

policy:
  allow_implicit_invocation: true
